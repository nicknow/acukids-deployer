# Validation workflow

Use the checks and target-system cautions in
[repository maintenance](../../docs/maintenance.md#validation). Record
task-specific commands and results in `.work/<agent-name>/`, not here. Promote
only generally reusable improvements to this workflow.
